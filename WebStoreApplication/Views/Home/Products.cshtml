@using System.IO;
@using System.Net;
@using Newtonsoft.Json;
@using Newtonsoft.Json.Linq;
@using WebStoreApplication.Global;

@{
    ViewData["Title"] = "Products";

    /*ProductModel test = new ProductModel();
    test.productName = "test";
    test.productDescription = "passed the test";
    test.productID = 1;
    test.price = 2;
    test.userID = 3;
    string json = JsonConvert.SerializeObject(test);
    String test1 = Global.Post("http://localhost:5000/products/", json, "application/json", "PUT");*/

    String result = Global.Get("http://localhost:5000/products/all");

    List<ProductModel> products = JsonConvert.DeserializeObject<List<ProductModel>>(result);




}


<h1>Products</h1>



<table class="table" id="productsTable">
    <thead>
        <tr>
            <th scope="col">Product Name</th>
            <th scope="col">Description</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (ProductModel product in products)
            {
                <tr>
                    <td>@product.productName</td>
                    <td>@product.productDescription</td>
                    <td>@product.price</td>
                </tr>
            }
        }
    </tbody>
</table>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('#productsTable').DataTable();
            $('.dataTables_length').addClass('bs-select');
        });
    </script>
}


